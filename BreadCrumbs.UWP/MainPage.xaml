<Page
    x:Class="BreadCrumbs.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BreadCrumbs.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BreadCrumbs.Shared.Models"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox x:Name="nameTextBox" HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="340" />
        <Button x:Name="saveButton" Content="Save" HorizontalAlignment="Left" Margin="10,47,0,0" VerticalAlignment="Top" Width="340" Click="saveButton_Click"/>
        <ListView 
            x:Name="savedPlacesListView" 
            HorizontalAlignment="Left" 
            Height="546" 
            Margin="10,84,0,0" 
            VerticalAlignment="Top" 
            Width="340">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Place">
                    <TextBlock Text="{x:Bind Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
